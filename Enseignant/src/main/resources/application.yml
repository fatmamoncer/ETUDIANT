spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/Enseignant
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver
server:
  port: 8081

